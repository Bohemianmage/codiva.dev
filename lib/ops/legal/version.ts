/**
 * Versión vigente del bundle legal (TyC + aviso + NDA de portal).
 *
 * Al cambiar de forma material:
 * 1. Actualiza el contenido en `lib/ops/legal/content.ts`
 * 2. Bump este código y `LEGAL_UPDATED_LABEL`
 * 3. En Ops → Configuración → publica la versión y opcionalmente notifica re-aceptación
 *
 * Los miembros del portal deben volver a aceptar cuando su `*_version` ≠ este valor.
 */
export const LEGAL_DOCS_VERSION = '2026.08.11';

export const LEGAL_UPDATED_LABEL = '11 de agosto de 2026';
